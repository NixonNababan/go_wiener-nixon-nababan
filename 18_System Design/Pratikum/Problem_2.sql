==Redis==
--Perintah untuk mengambil semua data dari Redis pada key users adalah HGETALL users.--
HGETALL users

==Neo4j==
--Perintah untuk mengambil semua data dari node User pada Neo4j adalah MATCH (u:User) RETURN u.--
MATCH (u:User) RETURN u

==Cassandra==
--Perintah untuk mengambil semua data dari tabel users pada Cassandra adalah SELECT * FROM users.--
SELECT * FROM users